import { ImageByCountryPipe } from './image-by-country.pipe';

describe('ImageByCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageByCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
